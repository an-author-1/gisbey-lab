# Reader feedback (informal, attributed)

Qualitative notes from the reader (the lab's operator), recorded as-given. These are
informal impressions, not structured review records — no numerical grounding/effect
scores have been assigned, and no proposal/bond has been accepted on the reader's behalf.
For the structured per-run review fields, see each run's own `review.json`/`review.md`
(left pending unless the reader fills them in directly via `gibsey review`).

## On the f12-micro run (F12.S4 → F12.S7)

> "The live micro run selected F12.S7 for F12.S4. I think that pairing works, but
> sentence-level evaluation feels tedious."

Reading: the pairing itself is endorsed informally. The friction is procedural — reviewing
connections at sentence granularity is more overhead than it's worth to the reader. This is
part of why page-level comparison (see below) is now the reader's stated focus, not sentence-
level Choice runs.

## On the f12-macro run (F12 → P8)

> "The macro run selected P8. I see a meaningful connection, but the repeated closing
> warning makes the flow feel redundant."

Reading: the reader agrees the F12/P8 shared closing sentence is a real, legible
correspondence — not disputing Jev's selection as ungrounded — but finds the *effect* of
reading it thin, since the second occurrence adds nothing to the first. This is a comment
on literary flow, separate from whether the correspondence is textually grounded (it is:
verbatim shared sentence).

## Standing preference going forward

> "Page-to-page comparisons are now our main focus."

The reader wants less procedural overhead (fewer commands, no JSON/hash inspection) and
more direct reading of candidate pairings, with interpretation clearly separated from Jev's
own recorded output. See `runs/20260919T195949Z_f12-macro_live/page_comparison.md` for the
first artifact built to that preference.
